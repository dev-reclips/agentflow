PORT=3000
NODE_ENV=development
DATABASE_URL=postgres://agentflow:agentflow@localhost:5432/agentflow_dev
JWT_SECRET=change-me-in-production
# Comma-separated list of valid API keys (empty = no auth enforcement, dev only)
API_KEYS=
