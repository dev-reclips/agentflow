PORT=3000
NODE_ENV=development
DATABASE_URL=postgres://agentflow:agentflow@localhost:5432/agentflow_dev
JWT_SECRET=change-me-in-production
# API keys are now stored in the database (api_keys table).
# Run `pnpm db:seed` after migration to create a dev key.

# Demo environment (sales demos)
# Set both to enable the /demo auto-login shortcut and POST /api/admin/demo/reset
DEMO_PASSWORD=
ADMIN_API_KEY=
